# Generated by `npm run build:css`, and built by the workflow rather than
# committed — the same reasoning as the app's stylesheet. A committed build
# output is one that can silently disagree with its source.
styles.css
node_modules/
